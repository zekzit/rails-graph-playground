<%= form_with(model: vehicle_comment, local: true) do |form| %>
  <% if vehicle_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_comment.errors.count, "error") %> prohibited this vehicle_comment from being saved:</h2>

      <ul>
        <% vehicle_comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
