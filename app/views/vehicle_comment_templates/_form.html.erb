<%= form_with(model: vehicle_comment_template, local: true) do |form| %>
  <% if vehicle_comment_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_comment_template.errors.count, "error") %> prohibited this vehicle_comment_template from being saved:</h2>

      <ul>
        <% vehicle_comment_template.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :score %>
    <%= form.text_field :score %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
